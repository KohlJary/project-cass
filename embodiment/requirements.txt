# Core display
pygame>=2.5.0

# Configuration
python-dotenv>=1.0.0

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24.0

# Voice activity detection (optional, falls back to energy-based)
webrtcvad>=2.0.10

# Wake word detection
openwakeword>=0.4.0

# WebSocket client
websockets>=12.0

# Async HTTP client (for direct TTS testing)
aiohttp>=3.9.0

# Speech-to-text
faster-whisper>=1.0.0

# HTTP client (for remote face database sync)
requests>=2.31.0

# Face recognition (for --face-recognition flag)
# Note: Requires dlib, which needs cmake
# sudo apt install cmake build-essential
opencv-python>=4.8.0
face-recognition>=1.3.0
setuptools<70  # Required for face_recognition_models pkg_resources
