# Cass Vessel TUI Frontend Configuration
# Copy this to .env and adjust as needed

# Backend server connection
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Optional: WebSocket URL (defaults to ws://{BACKEND_HOST}:{BACKEND_PORT}/ws)
# WS_URL=ws://localhost:8000/ws

# Optional: HTTP timeout in seconds
# HTTP_TIMEOUT=30

# Optional: Default project to load on startup
# DEFAULT_PROJECT=
