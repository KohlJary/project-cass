# Cass Smart Speaker - Python Dependencies

# Core
websockets>=12.0
numpy>=1.24.0

# Audio recording/playback
sounddevice>=0.4.6
soundfile>=0.12.0
PyAudio>=0.2.13  # Fallback if sounddevice fails

# Wake word detection
openwakeword>=0.5.0

# Local STT (optional - can use backend STT instead)
# Note: openai-whisper is heavy, consider whisper.cpp for Pi
openai-whisper>=20231117; platform_machine != "armv7l"

# ReSpeaker LED control (optional)
# pixel-ring  # Install separately from respeaker repo

# HTTP client (for future REST endpoints)
aiohttp>=3.9.0
